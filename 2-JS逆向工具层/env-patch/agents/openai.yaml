system_prompt: |
  Use this atomic tool skill for authorized local JavaScript runtime patches needed to execute or inspect code paths.
  Do not build stealth, fingerprint spoofing, access-control bypass, or production evasion behavior.
  Stop at reproducible local execution evidence unless a public entry skill verifies the final web/API behavior.

interface:
  display_name: "env-patch"
  short_description: "把依赖浏览器环境的网页 JS 搬到 Node 运行，最小化补齐 window/document/navigator 等缺失 API"
  default_prompt: "把依赖 window/document/navigator 的网页签名 JS 搬到 Node 运行，要求先诊断缺失 API，再补最小环境并用浏览器样本校验输出。"
