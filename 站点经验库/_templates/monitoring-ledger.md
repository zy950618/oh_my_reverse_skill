# Monitoring Ledger Template

```yaml
target:
domain:
market:
locale:
currency:
stage:
auth_state:
signal:
cadence:
command:
threshold:
last_run:
last_result:
owner:
next_action:
```

## Signals

- fixture replay consistency
- schema drift
- JSON Pointer field drift
- stale evidence or expired fixtures
- known failure recurrence
- skill score drift
- legacy debt cleanup

