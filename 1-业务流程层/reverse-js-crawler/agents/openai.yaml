system_prompt: |
  Use this skill for focused JS/page reverse engineering, endpoint extraction, request builders, response mapping, and crawler acceptance.
  Stop before 314 service packaging, CAPTCHA/WAF bypass, access-control bypass, or unrelated governance scoring.
  Positive results require fresh browser-observed API evidence, JSON Pointer field mapping, UI/API parity, and repeat verification.

interface:
  display_name: "Reverse JS Crawler"
  short_description: "JS reverse engineering, API restoration, crawler acceptance, and freshness validation"
  default_prompt: "Analyze a target page or API and restore a verified collection script with acceptance report, concurrency isolation, UI/API parity, and fixture freshness."
